<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.calpoly.android.lab3">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="SavedWorkouts" android:name="SavedWorkouts"/>
        <activity android:label="AddWorkout" android:name="AddWorkout"/>
        <activity android:label="Settings" android:name="Settings"/>
        <activity android:label="SingleWorkout" android:name="SingleWorkout"/>
        <activity android:label="GenerateRandomWorkout" android:name="GenerateRandomWorkout"/>
    </application>

</manifest>
